<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Irregular_Sliding_Puzzle.DesignGame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Canvas SizeChanged="UpdateSize">
        <ScrollView x:Name="daughter">
            <Grid
                Padding="304,112,32,32"
                VerticalAlignment="Top"
                HorizontalAlignment="Left">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel
                    Grid.Row="1"
                    x:Name="mother"/>
                <StackPanel
                    Grid.Column="1"
                    x:Name="father"
                    Orientation="Horizontal"/>
                <Grid
                    Grid.Row="1"
                    Grid.Column="1"
	                x:Name="baby"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Left"
                    PointerPressed="DragStart"
                    PointerReleased="DragEnd"/>
            </Grid>
        </ScrollView>
        <Grid
            Canvas.Left="288"
            Canvas.Top="16"
            Canvas.ZIndex="1"
            x:Name="son"
		    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
		    CornerRadius="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border
                x:Name="write"
                Padding="8"
                CornerRadius="4,0,0,4"
                BorderThickness="1"
			    VerticalAlignment="Center"
                HorizontalAlignment="Right"
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                Tapped="AsWrite">
                <FontIcon Glyph="&#xE70F;"/>
            </Border>
            <Border
			    Grid.Column="1"
                x:Name="reverse"
                Padding="8"
                CornerRadius="0"
                BorderThickness="1"
			    VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                Tapped="AsReverse">
                <FontIcon Glyph="&#xE895;"/>
            </Border>
            <Border
			    Grid.Column="2"
                x:Name="erase"
                Padding="8"
                CornerRadius="0,4,4,0"
                BorderThickness="1"
			    VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                Tapped="AsErase">
                <FontIcon Glyph="&#xE75C;"/>
            </Border>
            <AppBarButton
			    Grid.Column="3"
			    VerticalAlignment="Center"
			    Icon="Play"
                x:Uid="Play"
                Click="StartGame"/>
        </Grid>
        <StackPanel
            Canvas.Left="16"
            Canvas.Top="16"
            Canvas.ZIndex="1"
            x:Name="records"
            Width = "256"
            Padding="16"
            Spacing="12"
            Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
            CornerRadius="8"
            Loaded="DisplayRecords">
            <TextBlock
                x:Uid="History"
                FontSize="18"
                FontWeight="Bold"/>
            <Border/>
        </StackPanel>
    </Canvas>
</Page>
